docker run -d pwntainer
docker exec -it <hash> bash

--------------

refactor recon scripts to use aar
	dig_deep
		add records to DB
	tld_scan
	udp_scan

create alias based api client
	get services
	get-domains-last-day

create README file
-------------------------------

cron jobs
	1. subdomain monitoring
	2. crawl new subdomains
	3. discover and monitor static assets (javascript, json, xml)
